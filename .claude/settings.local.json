{
  "permissions": {
    "allow": [
      "Bash(chmod +x /Users/gugufufu/goodoc-demo/mvnw)",
      "WebSearch",
      "Bash(./mvnw compile -q 2>&1)",
      "Bash(git pull --rebase origin main && git push origin main && git checkout feature/add-cancel-reservation && git stash pop 2>&1)",
      "Bash(git stash && git pull --rebase origin main && git push origin main && git checkout feature/add-cancel-reservation && git stash pop 2>&1)"
    ]
  }
}
